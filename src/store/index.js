import { createStore } from "vuex";
import currencies from './modules/currencies/actionCreator';

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    currencies,
  },
});
